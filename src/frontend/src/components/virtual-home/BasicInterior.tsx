import { RealisticHomeModel } from './RealisticHomeModel';

/**
 * Interior component that renders the realistic 3D home model.
 * Maintains compatibility with existing imports while providing the new detailed environment.
 */
export function BasicInterior() {
  return <RealisticHomeModel />;
}
